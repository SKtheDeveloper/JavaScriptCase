// in this file we understand different methods of array
//to check this out plz uncomment code

let numbers = ['One', 'Two', 'Three', 'Four', 'Five']

//numbers[1] = 'Something'    // it changed index position [1] value to something

//console.log(numbers);       //[ 'One', 'Something', 'Three', 'Four', 'Five' ]

// array.shift() => method shift index by 1 number so the value on index 0 ..not printed
// array.unsift('any string') => method insert a new value at the index 0 position
// array.pop() => method delete last value of array
// array.push('any string') => method insert a new value at the last index  position
/* array.splice(2,1,"something") => method delete value from index 
                                    that is given ..here is 2. in method
                                    next value tells how many values you 
                                    want to delete.. here is 1. next a string 
                                    or a number that insert at the given 
                                    index position ..here is 2.
*/

